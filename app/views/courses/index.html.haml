%p#notice= notice
%h1 Courses
%table
  %thead
    %tr
      %th{:colspan => "3"}
  %tbody
    - @courses.each do |course|
      %tr
        %h4.td= link_to course.name, course
      %tr
        %td= link_to "Edit Course", edit_course_path(course)
        %td= link_to "Delete Course", course, method: :delete, data: { confirm: 'Are you sure?' }
%br/
%h4
  = link_to 'Create a New Course', new_course_path
